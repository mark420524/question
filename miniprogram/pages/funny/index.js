Page({
    data:{},
    onLoad(){
        /*
        wx.cloud.callFunction({
            name: "querytv"
        }).then(res=>{
            console.log(res.result)
        }).catch(console.error)
        */
    }
})